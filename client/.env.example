# During local dev, point links to the Express app that still serves legacy HTML (port from .env or 8080).
VITE_LEGACY_BASE=http://localhost:8080

# Vite dev server: open http://localhost:5173/app/ (basename /app matches production).
